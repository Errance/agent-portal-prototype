# agent-portal 环境变量样例。复制为 .env.local 或在 CI 里注入。
# 注意：VITE_ 前缀变量会进前端 bundle，不能放机密。

# mock 开关
#   true  → 总是 mock
#   false → 总是走真实 API
#   不设置 → DEV 默认 mock，生产默认真实 API（推荐）
# VITE_USE_MOCK=

# 真实 API 基址（VITE_USE_MOCK=false 或生产默认时生效）
# VITE_API_BASE=/api

# 路由类型：留空 = HashRouter（适合 GitHub Pages 子路径部署）
#          browser = BrowserRouter（需要 nginx try_files 或等价 SPA fallback）
# VITE_ROUTER=

# 构建 base 路径（由 GitHub Actions repository variable 注入）
# 本地开发不需要设置
# VITE_BASE=/agent-portal-prototype/

# Privy App ID（与主站共用；接入前可留空）
# 参考主站 PrivyAppID 切换逻辑：website/web/src/PrivyWalletProvider.tsx
# VITE_PRIVY_APP_ID=
